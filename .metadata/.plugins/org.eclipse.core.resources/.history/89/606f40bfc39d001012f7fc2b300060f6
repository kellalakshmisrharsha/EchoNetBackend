package com.app;

import java.util.List;

import org.springframework.data.jpa.repository.JpaRepository;

public interface FriendRepo extends JpaRepository<Long,Friend>{
	public List<Friend> findByUser(User user);
}
